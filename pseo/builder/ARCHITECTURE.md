# Карта: мозг ключей → потребители

Единственный источник этой карты. **Меняешь код ключей/потребителей — правишь ЭТУ таблицу в том же коммите.** Чтобы не грепать заново каждый раз.

## Термины

- **сиська** = `keybroker.py` — держит молоко: ключи, квоты (`usage`), часы (`key_clock`), per-key cooldown (бан→разбан). Ключи живут ТОЛЬКО здесь.
- **сосок** = `keybroker.call(user, sysprompt, consumer, model, role)` — **одна** дверь к Gemini: `acquire → HTTP → report → разбор JSON`. Молоко течёт через неё всем. Рот получает `dict`, ключа не видит.
- **рот** = call-site потребителя, где он присасывается к соску. Рот называет себя (`consumer`) и молоко (`model`).
- **драйвер** = кто спавнит потребителя процессом (`runner`/`lang_runner`/`batch`). Спавн обнуляет in-memory такт → темп держит только персистентный `key_clock` в сиське.

## Живые рты (есть драйвер) — 7. Экстрактор НЕ здесь (см. ниже)

Все на `gemini-3.1-flash-lite`, роль `background`.

**⛔ КАПЫ РТОВ СНЯТЫ (решение юзера 2026-07-21: «убрать капы и измерить реальные запросы»).**
Числа капов ниже были моими оценками на глаз («×3») — юзер их не принимал; вчера kratko и
translate упёрлись в эти выдумки при полупустых ключах. Enforcement в `acquire` вырезан;
учёт `consumer_usage` ЖИВ и остаётся единственной правдой о реальном расходе ртов.
Заборы теперь: per-ключ RPD + такт очереди. Таблица `consumer_cap` — задел, не действует.
Колонка «кап» ниже — исторические оценки, оставлены как ориентир объёмов:

| рот (`consumer`) | файл | батч | кап RPD/день | основа | драйвер |
|---|---|---|---|---|---|
| `facet` (`facet_one`) | `facet.py` | 1 муха | **~1500** | замер: пик 482 ×3 | `runner` |
| `translate` (`translate_texts`) | `facet_lang.py` | **50 мух** | **~400** | 482×13яз/50 ×3; throughput 13/мин·ключ (канон 4.1) | `lang_runner` |
| `questions` | `questions_page.py` | 15 мух/запрос | **~300** | оценка ~100/день ×3, замерить | `runner` |
| `carve` (экс-consolidate, переим. 07-19) | `facet.py` `carve_family` | ≤90 мух-ТЕКСТОВ | **~300** | оценка | `runner` |
| `assign` | `facet.py` `assign_tail` | ≤90 мух-ТЕКСТОВ | **~100** | ceil(хвост/90)×≤3 ретрая на гео (br≈63) | руками `facet.py <geo> --assign-tail` |
| ~~`faq`/`synth`~~ | ~~`page_builder.py`~~ | — | — | МЕРТВЫ: тупиковая ветвь build/page_builder закрыта 07-19 (BUILDER_RULES) | нет |
| `labels` (`translate_labels`) | `facet_lang.py` | 60 меток | **~200** (дефолт) | копейки | `lang_runner` |
| `kratko` (`kratko_for`) | `dedup.py` | ≤12 абзацев/вид | **~300** | 1 вызов/вид-страницу; br=134, бэкфилл-разовый | руками (`dedup.py <geo> --kratko`) |

`DEFAULT_CONSUMER_CAP` больше не применяется (капы сняты, см. выше).

### Бэкфилл ≠ daily (основа капов)

Все рты едят **муху** (`ai_lesson` ≈ 354 симв ≈ **88 ток**, замер 2026-07-16, 14 288 шт).
- **Дневной приток мух ≈ 300/день** (замер: 191–482). При 1 мухе/запрос это ~300 facet-запросов/день = **~25 на ключ из 500 RPD → крохи.** В steady-state ВСЕ рты тривиальны.
- **Большие объёмы (facet 14 288, translate ×языки) — РАЗОВЫЙ БЭКФИЛЛ**, не дневная нагрузка. Потому дефолтный кап мал (дневной пик ×3), а бэкфилл — явный временный подъём.
- Оптимум батчей поэтому нужен **только ради скорости бэкфилла**, не для дневной работы.

## Оптимизация ртов — что можно, что нет (замер 2026-07-16)

Окно запроса = `TPM/RPM` = 250К/15 ≈ **16.6К ток**. Плотность = сколько окна грузит запрос.

| рот | плотность | плотнее? | совместить? | вердикт |
|---|---|---|---|---|
| `facet` | 1 муха = **1.7%** окна | да — N мух/запрос (50 ≈ 75% окна, токенно влезает) | RU-перевод УЖЕ внутри (`perevod` в одном вызове с тегами) | ⚠️ ЕДИНСТВЕННЫЙ рычаг, но **упирается в качество map** (модель плывёт на N наборов тегов — `consolidate` это и предупреждает). Мерить контролем, не ставить 50 наугад. Только для бэкфилла |
| `translate` | 50 мух = 60% (+выход 10К) | нет — загружено | — | оставить |
| `write_faq` | ≤20 мух = 13% | нет — **батч = размер секции** (структурно) | слить секции = смешать темы | оставить |
| `synth` | ~6% | нет | после `write_faq` по природе (сжимает FAQ) | оставить |
| `questions` | 15 мух = 9% | да, до ~100 (окно терпит) | можно вложить в `facet` (та же муха) | выигрыш ~100 запросов = копейки, трогать только если и так лезем в facet |
| `consolidate`→**`carve`** | ✅ **ЗАМЕНЁН** (2026-07-18): читает ТЕКСТЫ мух по фасет-семье, вычленяет подпункты | зовётся на дозревшем гео, батч ≤90 мух/семья | — | **carve > дедуп меток** (br/vn: метка degraded, текст исходник; держит ВНЖ≠гражданство). ГРАНИЦА: только плотная семья ≥MIN_CARVE; тонкий хвост как facet |
| `labels` | метки, малы | нет (map-качество) | — | крохи |

**Итого: жертвовать нечего.** Уже плотно/совмещено: `translate` (50), `facet` (перевод+теги в одном проходе — RU-перевод бесплатный прицеп). Структурно связаны: `write_faq`/`synth`. Единственный реальный рычаг — **батч `facet` на бэкфилле**, качество-gated (контроль PIX). Кандидат на совмещение — вложить `questions` в `facet` (одна муха → теги+перевод+вопрос), но выигрыш мал.

## Очередь пула — держит МОЗГ (канон юзера 2026-07-20)

Раннеры снесены как класс (PROCESSES.md) — прежний пункт «очередь держит раннер» мёртв.
**Очередь держит сам мозг: ТАКТ НА ВЫДАЧУ.** `acquire` отдаёт ключи по одному и не чаще
раза в `GRANT_STEP` (дефолт 3с, ручка юзера `KB_GRANT_STEP`) на весь пул — метка
последней выдачи в `broker_global.busy_ts`. Полёт (HTTP) пул НЕ держит: «рот получил
ключ и пошёл — сосок ему не нужен» (юзер); длинные translate-батчи никого не блокируют.

- Частота ЗАПУСКОВ с IP ограничена тактом железно: 3с = ≤20/мин. Точного по-IP порога
  НЕ существует (не публикован); факт — общее окно 429 ловилось при 22–45 grants/мин
  (инциденты 07-19/20, параллельные рты). Одновременность полётов частоту не создаёт.
- Ожидание такта НЕ тратит бюджет вызова (из очереди не уходят); порядок — по факту
  прихода, без приоритетов.
- ≠ GLOBAL_FLOOR из build.py: тот был пер-процессным «полом» (осьминог), вычищен
  правильно; такт живёт в общей SQLite — один на все процессы.
- Зависимости по данным (carve после facet) держит ПОРЯДОК ЗАПУСКА прогонов, не мозг.

## Мёртвый код (рты в файле есть, драйвера НЕТ — НЕ присасывать)

| файл | ртов | почему мёртв |
|---|---|---|
| `shelf.py` | 3 | труп (single-bucket, роадмап) |
| `facet_en.py` | 1 | никто не зовёт |
| `regroup.py` | 1 | разовый черновик, `br` литералом |

Оживёт (появится драйвер) — тогда строка переезжает в живую таблицу.

## Вне сиськи (не путать)

- **Экстрактор** — второй потребитель ТЕХ ЖЕ ключей и модели, свой пейсинг, **свой деплой (бот)**. К мозгу подключаем ПОЗЖЕ — только после того, как связка билдер+рты доказана. Пока не считаем и не трогаем.
- **Эмбед** — своя модель (`gemini-embedding-2`), свой RPD, своя логика. Не в пуле.
- **Live (Luky)** — RPD безлимит, своя модель.

## Учёт по рту (капы СНЯТЫ юзером 2026-07-21)

Enforcement капов ртов вырезан из `acquire` («убрать капы и измерить реальные запросы»).
Что осталось и работает:

1. Рот присасывается, называя `consumer`.
2. На выдаче — `consumer_usage[consumer]++` (та же транзакция, рядом с per-ключ `usage`) —
   это и есть замер реального расхода каждого рта по PT-дням.
3. Защита пула: per-ключ RPD + per-ключ шаг/cooldown + такт очереди. Всё.

Смотреть расход: `sqlite3 keybroker.db "SELECT consumer,count FROM consumer_usage WHERE pt_day='<PT-день>'"`.

```sql
consumer_cap(consumer PK, rpd_cap INT, rpm_cap REAL)          -- задел, НЕ enforce (капы сняты)
consumer_usage(consumer, pt_day, count, PK(consumer,pt_day))  -- счёт дня, ++ на выдаче (ЖИВ)
```

- `set_cap(consumer, rpd_cap)` — задать кап (числа юзера). `CAPS` + `seed_caps()` засевают значения из таблицы выше; `__main__` сеет при init.
- Проверено: кап 3 → ровно 3 выдачи, дальше отказ; `cap_block` пишется; засеяно 7.

### Логирование аномалий (говно видно в `stats()`)

`request_log.event` пишет не только `grant`/`report`, но и:

- `cap_block` — рот упёрся в свою долю (сигнал: рот тяжёлый/сломан);
- `parse_fail` — пришёл 200, но JSON-мусор (раньше выглядел успехом);
- `429_env` — 429-волна СРЕДЫ (другой ключ словил 429 в окне 30с): ключ НЕ наказан
  (механизм вина-vs-среда, 2026-07-20 — внешний чих больше не прожигает пул в cooldown);
- (глоб-`abuse_pause` вычищена, §канон 2.5 — залп 429 гасится per-key cooldown, не паузой пула).

**Тело не-200 → `error_bodies.log`** (рядом с `keybroker.db`). `call` на ЛЮБОЙ не-200 (400/429/5xx/сеть)
пишет `ts\tstatus\tconsumer\tmodel\tтело[:800]`. Зачем: в `request_log` тела НЕТ, а причина отказа
(RESOURCE_EXHAUSTED? INVALID_ARGUMENT? SAFETY?) — только в теле. **Замер 2026-07-18:** 400 ≈ 429 по числу
(12/12 за сутки), сидят на `consolidate`, НЕ размер (120 меток/90 мух/350 мух-47К-ток → все 200) — тело покажет.

⚠️ **Остаток:** `RPM_DIVISOR=4` (шаг 16с) ещё в коде — под-обслуживает перевод (положено 13/мин). Выпил + темп от нормы рта — отдельный шаг, при проводке translate. Кап это не трогает.

## Статус проводки (меняется по мере работы)

- **Сосок (`call`)** — построен (Шаг 1). Ключи в мозге, `acquire/report` не тронуты.
- **Рты присосаны (✅ Шаг 2)** — `facet`/`facet_lang`/`questions_page`/`page_builder` зовут `keybroker.call(..., consumer=…)`, `from build import gemini_json` убран. translate батч 8→50. **НЕ запускалось на боевых ключах** (нужен worst-case + разрешение юзера).
- **Кап по рту** — ✅ построен + засеян + логирование аномалий (см. выше). Остаток: выпил `RPM_DIVISOR=4` при проводке translate.
- **Экстрактор** — не подключён (по плану, позже).

## Инварианты

- Ключи — только в сиське. Рот ключ не получает никогда (ниппель).
- Один сосок. Обойти = взять ключ мимо `call` = запрещено (иначе осьминог обратно).
- `usage` считает расход билдера+ртов. Когда придёт экстрактор — переключить на общую `gemini_quota` (его расход тоже виден). Пока не нужно.
- TPM в сиське НЕ считается: батчи под окном 16.6К (`TPM/RPM`), RPM упирается первым. Счёт токенов понадобится только если батч перевалит за окно.
