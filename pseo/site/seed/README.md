# Семя «что знает мир» (снято 2026-08-19)

Здесь лежит только то, что **нельзя восстановить**. Всё производное — командой, не файлом.

## `known_urls.tsv` — 188 адресов, 550 показов, 1 клик

Выгрузка Search Console за 3 месяца, колонка Top pages. Формат: `путь<TAB>показы<TAB>клики`,
порядок по показам. ⛔ Невосстановимо: окно консоли — 16 месяцев.

Зачем: это единственный источник приоритета при переезде адресов. Цель редиректа для адреса с
показами обязана быть **ответом**, а не оглавлением страны — в нынешней карте 983 правила из
992 ведут в хаб, и именно поэтому 448 показов дают ноль кликов.

## `redirects_2026_08_19.txt` — снимок 996 действующих правил

Копия `_redirects` из pages-репо на день снятия. Нужна как семя новой карты: старые правила
не выбрасываются, а перенацеливаются.

## Список выложенных адресов — НЕ файлом, а командой

38 800 адресов воспроизводятся из истории pages-репо, поэтому копию в репозиторий не кладём:

```bash
git -C ../../multyspeak-pages show HEAD:sitemap.xml | grep -o "<loc>[^<]*" | sed "s|<loc>||"
```

## Чего здесь нет

Списка «обнаружено, но не проиндексировано» — консоль его не отдаёт выгрузкой. Число
(1 805 обнаружено на 13 июля) есть только в отчёте о картах сайта, скриншотом.
