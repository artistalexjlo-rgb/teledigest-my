# План: что во что превращается — шаг за шагом

Состояние на 2026-08-25. **Это единственный документ тракта.** Старый канон
(`BUILDER_RULES.md`) и описание старого сайта (`SITE_MAP.md`) уехали в `pseo/legacy/`: они
описывают отменённую схему, и перенос строк оттуда уже дал противоречие — «части внутри
страницы, адрес один» против «страница не больше 15 абзацев». Сюда пишем решения юзера и
замеры, а не цитаты из архива.

Формат каждого шага одинаков:
**ВХОД** (что и сколько) → **ЧТО ДЕЛАЕМ** (рот и о чём просим, или код) →
**ВЫХОД** (какие именно поля) → **КУДА и ЗАЧЕМ**.

⛔ **Каждый вызов представляется соску СВОИМ именем** (список ЗАКРЫТ —
незнакомое имя мозг отвергает): `mark` (звено 3) · `canon` (4А) · `assign` (4Б) ·
`branch` (5, ветвление) · `translate` и `labels` (6) · `extract` (бот).

✅ **Сделано 25.08:** звенья 3 и 4 зовут соска своими именами, реестр капов приведён к
закрытому списку, мёртвые потребители (`facet`, `carve`, `questions`, `faq`, `synth`) сняты.
⚠️ **Осталось:** модель и таймаут звенья не передают — берётся дефолт соска.

**Ключи тратят только звенья 3, 4 и 6.** Схлопывание, сборка и публикация — код, бесплатно.
Счёт вызовов на одну страну (Греция, 751 совет после схлопывания): разметка **31** · проход
А **13** (по одному на тему) · проход Б **9** (пачки по 90) · переводы — по числу языков и
страниц.

**Слова.** **Тема** — плитка хаба, их 13, ставится в звене 3. **Подтема** — короткая метка
совета оттуда же; это СЫРЬЁ для прохода А, а не заголовок сайта. **Страница** — имя из
закрытого списка звена 4, вот оно и становится заголовком и адресом. **Остаток** — советы,
которым имени не нашлось: списком под кнопками страниц на странице темы.

---

## Схема страниц: что получается на выходе

```text
/<язык>/                                    главная: список стран
  /<язык>/<страна>/                         ХАБ страны: 13 плиток тем
    /<язык>/<страна>/<тема>/                ТЕМА: кнопки страниц + остаток списком ниже
      /<язык>/<страна>/<тема>/<страница>/   СТРАНИЦА: советы

служебные:  /<язык>/go/luky/   шлюз клика в продукт (noindex, в карту сайта не идёт)
            /<язык>/find/     поиск по заголовкам
            /<язык>/about/    о проекте
```

**Хвост одинаковый во всех языках** (правило юзера: адрес принадлежит СОДЕРЖИМОМУ, а не
переводу): `<тема>` — английский ключ таксономии (`visa`, `transport`,
`housing`), `<страница>` — слаг английского имени из справочника. То есть
`/ru/gr/visa/visa-processing-time/` и `/en/gr/visa/visa-processing-time/` — один и тот же
адрес, отличается только язык содержимого.

⛔ **Тема сама по себе текста не несёт**: на её странице кнопки страниц и остаток. Абзацы
живут только на конечной странице, и их там от `PAGE_MIN`. Страниц в теме 5–15 — по числу
имён закрытого списка звена 4.

⛔ **Страница не бывает толще 15 абзацев** (правило юзера). Имя, набравшее больше, режется
по порогу: 38 пунктов → три страницы 15 / 15 / 8. Часть — самостоятельная страница со своим
адресом: `…/visa-documents/`, `…/visa-documents-2/`, `…/visa-documents-3/`. Хвост короче
`PAGE_MIN` страницей не становится — уходит в остаток темы.

**Кто что ставит и куда оно попадает:**

| что | кто ставит | куда попадает |
|---|---|---|
| **тема** — один ключ из 13 | звено 3 | сегмент `/<тема>/` и плитка на хабе страны |
| **подтема** — 2–6 слов | звено 3 | **в адрес не попадает**: сырьё для прохода А, наружу не выходит |
| **имя страницы** — из закрытого списка | звено 4, проход Б | сегмент `/<страница>/` (слаг) и заголовок |
| **остаток** — советы без имени | звено 5, код | список внизу страницы темы |
| **счётчик «✓ N из чатов»** | звено 2 | у каждого абзаца на странице |

⚠️ **Выложено сейчас другое** — `/<язык>/<страна>/s/<раздел>/` и плоский
`/<язык>/<страна>/<разбор>/`. Переезд идёт ОДНОЙ миграцией; редиректы со старых адресов не
делаем (решение юзера 20.08: за три месяца старые адреса дали один клик).

---

## Сквозное правило всех вызовов рта: рту идут НОМЕРА, не id

Действует на каждом шаге, где рот получает пачку советов (3, 4, 6). Записано отдельно, потому
что живёт не в одном шаге, а во всех.

- **ВХОД:** пачка нумеруется с нуля — `{"0": "<текст>", "1": "<текст>", …}`. Номер позиционный,
  в пределах ОДНОГО запроса. Настоящий id мухи роту не показывается никогда.
- **ВЫХОД:** рот называет тот же номер. Код склеивает номер → id по списку, из которого он эту
  пачку собрал.
- **ЗАЧЕМ.** id мухи — 24-символьный хеш. На пачке в 90 советов это около двух тысяч лишних
  знаков в запросе и столько же в ответе, и каждый знак — возможность опечатки, которую нечем
  проверить: испорченный хеш выглядит как нормальный. Номер 0–89 короток, а ошибка в нём
  видна сразу — такого номера в пачке нет.
- **СКЛЕЙКА ОБРАТНО, три случая, и все решает код:**
  1. номера нет в пачке → пункт не берётся, молча;
  2. номер назван дважды → берётся первый; совет обязан лежать ровно в одном месте;
  3. номер не назван вовсе → совет остаётся необработанным и попадает в работу шага заново
     (несделанное — это очередь шага, а не строка в логе).
- **ГДЕ УЖЕ ТАК:** звено 3 с самого начала: `{"0": текст}` → ответ с тем же `i`.

⛔ **Нарушено 22.08 в звене 4:** роту ушли настоящие id — и в тексте запроса, и обратно в
«исключениях». Работать работало, но стоило лишних токенов на каждой мухе и давало
непроверяемый класс ошибок. Правило записано ЗДЕСЬ, чтобы следующая реализация не решала
этот вопрос заново.

---

## 1. Векторы — код, bge-m3 на своём сервере

- **ВХОД:** советы из базы бота (`extracted_patterns`, 26 241 с текстом), **у которых вектора
  нет**.
- **ЧТО ДЕЛАЕМ:** считаем локальной моделью bge-m3, 1024 измерения, нормированные.
- **ВЫХОД:** строка в `/root/embed_ab/local_vec.db`.
- **КУДА:** в схлопывание и во всё, где нужна близость по смыслу.

⚠️ **Дефект на 20.08:** выборка идёт «новее метки времени», а метка при потере файла состояния
ставится на максимум. Поэтому **1 575 советов без вектора не будут взяты никогда**. Чинится
переходом на «у кого нет вектора»; список сделанных скрипт и так читает.

## 2. Схлопывание почти-копий — код, порог 0.93

- **ВХОД:** советы гео с векторами.
- **ЧТО ДЕЛАЕМ:** группируем по близости, в группе выбираем представителя.
- **ВЫХОД:** у совета признак представителя и размер его группы.
- **КУДА:** разметка платится только за представителей; размер группы становится счётчиком
  «✓ N из чатов» на странице.

⚠️ **Замер 20.08 по Черногории:** при 0.86 схлопывается 23% — и это НЕ копии, в одну группу из
44 советов слиплись туристический налог, обязанность регистрации и ответственность владельца
жилья. При 0.93 схлопывается 3,5% — настоящие почти-копии. Дословных повторов в корпусе всего
полпроцента, поэтому хеш по тексту тут бесполезен.

## 3. Разметка — рот, пачка 25. НА АНГЛИЙСКОМ ✅ в коде с 25.08

- **ВХОД:** текст представителя группы — английский оригинал совета, как он лежит в базе.
- **ЧТО ДЕЛАЕМ:** просим **тему** из закрытых тринадцати и **подтему** в 2–6 слов,
  **по-английски**. Перевода НЕ просим. Имя-рубрика («transport», «money») запрещено: это
  тема, а не подтема.
- **ВЫХОД:** `id`, тема, подтема, счётчик схлопывания. **Текст в тегах не хранится** —
  он в базе, звенья 4 и 5 читают его по id (бесплатно).
- **КУДА:** `/root/pseo_builder/tags/<гео>.json`. Единственное место, за которое ключи
  уплачены безвозвратно — его и держать в бэкапе.

⭐ **ПОЧЕМУ ПО-АНГЛИЙСКИ.** Замер 23.08 по базе: из **24 807** советов с текстом кириллических
**ноль** — экстрактор пишет по-английски. Значит русский перевод в этом звене был рабочим
языком СТРУКТУРЫ, и это давало три беды сразу:

1. платили за перевод ВСЕГО корпуса, включая то, что уйдёт в остаток и на сайт не попадёт
   (Греция: перевели 751, на страницы попало 612 — 139 переводов впустую);
2. имя страницы получалось русским, а из него — адрес, то есть транслит
   (`sroki-rassmotreniya-vizy`) вместо английского хвоста, которого требует;
3. русский приходил на сайт двумя разными путями: тексты — переводом здесь, заголовки —
   переводом в звене 6.

Теперь структура целиком английская, а **русский — обычный язык перевода наравне с
остальными**: платим за него только там, где совет попал на страницу.

## 4. Обобщение — рот, ДВА ПРОХОДА на тему ✅ в коде с 25.08

⛔ **Почему два, а не один.** Прогон Греции 22.08 сделан одним вызовом «сведи названия» — и
дал слипание по широкому слову: в страницу «документы на визу» ушли **33 разные подтемы**
(биометрия, ч/б печать, выписки за 3 месяца, скрепки), 40 советов при потолке 15. Причина не
в лени рта: имя подтемы ставится в звене 3 **по одной мухе**, самое частое слово темы виз —
«документы», и свод по СХОДСТВУ ИМЁН честно слил всё, где это слово есть. Лечится тем, что
имя рождается не из одной мухи, а из списка, и список **закрыт**.

⛔ **Промпты НЕ СОЧИНЯТЬ.** Оба прохода уже написаны и бегали в старом тракте —
`facet.DEALS_SYS` и `facet.DEAL_ASSIGN_SYS`. Берём их дословно, меняя ровно три вещи: язык на
английский, слова «раздел/дело» на «тема/страница», id советов на номера. Ниже — что в них
уже есть, чтобы следующая реализация не изобретала это заново.

### Проход А — ЗАКРЫТЫЙ СПИСОК ИМЁН темы (`DEALS_SYS`)

- **ВХОД:** подтемы темы **с массами** (`номер: метка (сколько советов)`) — короткие строки,
  не тексты. Для темы виз Греции это ~400 строк вместо 150 тыс. знаков.
- **ЧТО ДЕЛАЕМ, дословно из старого промпта:** составить ЗАКРЫТЫЙ список того, с чем человек
  реально приходит; имя = ОДИН запрос человека, оно станет ЗАГОЛОВКОМ страницы; разные
  формулировки одного дела — ОДНО имя; разные дела НЕ смешивать (студенческая ≠ рабочая виза;
  CPF ≠ гражданство ≠ ВНЖ); запрещены «Прочее», «Разное», «Общие советы», «Полезная
  информация», «Особенности» без предмета и имя, совпадающее с названием темы.
- **ВЫХОД:** список имён, **от 5 до 15** (`NAMES_MIN`, `NAMES_MAX` — границы из старого
  промпта, `DEALS_MAX = 15`). Имена английские, поэтому **адрес получается слагом имени**
  и транслита не возникает как класса.

### Проход Б — ПРИСВАИВАНИЕ по закрытому списку (`DEAL_ASSIGN_SYS`)

- **ВХОД:** список имён (`номер: имя`, **нумерация с ЕДИНИЦЫ** — «0» занят под «ни одно не
  подошло») плюс советы темы, пронумерованные (сквозное правило).
- **ЧТО ДЕЛАЕМ, дословно из старого промпта:** отнести КАЖДЫЙ совет ровно к ОДНОМУ имени ИЗ
  СПИСКА, новых не придумывать; **не подходит ни одно — ставим «0»**, и это прямой путь в
  остаток.
- **ВЫХОД:** номер совета → номер имени.
- **ПАЧКИ ЗДЕСЬ БЕЗОПАСНЫ.** Тексты не влезают в один запрос, поэтому Б идёт пачками; имена
  при этом не плодятся — они зафиксированы проходом А.

⛔ **КОРОТКИХ ОТВЕТОВ НЕТ** (решение юзера 26.08: «нет в них смысла»). Прохода В не будет,
плашка над советами не собирается, потребитель `kratko` из списка снят. На старом сайте она
была; в новом тракте страница начинается сразу с советов.

**Контроль ДО прогона по корпусу** (правило «проверь метод, а не надежду», перенесён из
снесённого): прогнать А+Б на паре `gr/visa`, где есть ручной эталон — 9 дел на 41
метку. Метод годен, если список выходит 8–12 имён, покрывает все метки и не содержит
сборных. Не сошлось — правим промпт прохода А, а не корпус.

⚠️ **Бесключевую группировку по общим корням слов проверяли и отвергли** (замер 14.08,
контроль те же 41 метка): лучший F1 = 0.37, ни одна настройка не воспроизводит девять
сюжетов. Не переоткрывать.

Правило вывода, его проверяет код: каждый совет **ровно в одном месте** — на странице или в
остатке. Верхнего предела пунктов НЕТ: страница толще `BRANCH_MIN` ветвится внутри себя
(звено 5), а не возвращается роту.

⚠️ Мухи при обобщении НЕ переписываются. Корпус хранит факты, справочник — решения, поэтому
ошибку обобщения правит рука в справочнике, а не перепрогон рта.

## 5. Сборка — код

- **ВХОД:** `tags/<гео>.json` и справочник.
- **ЧТО ДЕЛАЕМ:** группируем советы по имени из звена 4; имя от `PAGE_MIN` пунктов становится
  страницей, остальное уходит в остаток своей темы. **Совет без имени идёт в остаток**, а не
  заводит корзину из своей подтемы. Адрес — `/<язык>/<страна>/<тема>/<страница>/`, английский
  хвост из справочника.

✅ **В коде с 25.08:** группировка по имени, остаток для безымянных, адрес из справочника.
⚠️ **Не сделано:** дерево в сборщике страниц — он по-прежнему строит старое
`/<страна>/<страница>/` с мостиком `/s/`.
- **ВЫХОД:** `out_facet/<гео>.json` — страницы и остатки тем.
- **КУДА:** в рендер. Файл **пересобираемый**: потерялся — собран заново без вызовов.

**ТОЛЩИНА СТРАНИЦЫ — РАБОТА КОДА, НЕ РТА** (решение юзера). Страница не бывает толще
`PAGE_MAX` = 15 абзацев. Пачка больше режется по порогу подряд, последняя часть неполная
(38 → 15 / 15 / 8). Каждая часть — отдельная страница; адрес второй и далее получает суффикс
`-2`, `-3`, иначе они молча затрут первую. Хвост короче `PAGE_MIN` — в остаток темы, по тому
же правилу, что и всё, чего на страницу не хватило.

⛔ Ровнять части (13/13/12) юзер не просил — это была моя выдумка, снята 26.08.

✅ **В коде с 26.08:** `podeli()` режет пачку по порогу, части становятся отдельными
страницами, хвост короче `PAGE_MIN` уходит в остаток. Проверка на выходе сборки перевёрнута:
страница толще порога — это СЛОМАННОЕ ДЕЛЕНИЕ, а не предупреждение.

- владелец числа ОДИН — `PAGE_MAX`; псевдонимов и литералов `15` по коду не заводим;
- ⛔ «страница толще 15» — это **сломанное деление**, а не предупреждение: после деления
  толстых страниц быть не может;
- деление механическое, ключей не тратит: рту тут делать нечего.

**Проверки, которые делает код на выходе звена 5:**

1. каждый совет ровно в одном месте — на странице или в остатке; сумма сходится с разметкой;
2. имя вне закрытого списка звена 4 → совет в остаток, имя не создаётся;
3. сборное имя (`bad_label`) страницей не становится никогда;
4. ⚠️ **предложение ассистента, не принято:** адрес не английский (транслит или кириллица)
   → страница объявляет `shared_tail=False`. В самом такой проверки нет, там
   описан только фолбэк слага. После перехода звена 3 на английский нужда в ней под вопросом.

## 6. Переводы — рот, пачка 50

- **ВХОД:** собранные страницы гео на английском — тексты советов (оригиналы) и имена страниц.
  **Только то, что попало на страницы**: остаток не переводим, за него не платим.
- **ЧТО ДЕЛАЕМ:** переводим на 14 языков, **включая русский** — он такой же язык перевода, как
  прочие, отдельного пути к нему нет. Английская версия собирается из оригиналов бесплатно.
- **ВЫХОД:** те же страницы на каждом языке; адрес общий (английский хвост), поэтому
  переключатель языка ведёт на ту же страницу.

## 7. Рендер и публикация — код

- **ВХОД:** корпус всех гео.
- **ВЫХОД:** дерево HTML, `sitemap.xml`, `robots.txt`, поиск по заголовкам.
- **КУДА:** выкладка полным снимком в pages-репо, кнопкой из пульта.

⛔ Редиректы со старых адресов не делаются (решение юзера 20.08): дерево меняется целиком, за
три месяца старые адреса дали один клик. Старое становится 404 и переиндексируется заново.

---

## Что осталось: что не так сейчас и как будет

Порядок сверху вниз — по цепочке. Сверено по коду 26.08.

### 1. Сборщик страниц ✅ СДЕЛАНО 26.08

Собран заново отдельным модулем `builder/site.py`; старый `pages.py` остался в стороне — он
строил отменённое дерево (мостик `/s/`, плоский адрес страницы, вопрос-контур, полки,
`subshelves`, поле `key`), и разбирать его 3 100 строк дороже, чем собрать четыре уровня
заново.

- дерево: главная → хаб страны с плитками тем → тема с кнопками страниц и остатком →
  страница с советами;
- адрес берётся из корпуса (`adres` от звена 4), заголовок на него не влияет — значит хвост
  одинаков во всех языках; нет адреса — страницы нет;
- звено 5 кладёт в страницу КЛЮЧ темы: он сегмент адреса, и сборщику незачем угадывать его
  обратно по человеческому имени;
- кнопка пульта и образ переведены на новый сборщик, сторожа тоже.

**Сквозной прогон 26.08:** корпус из двух тем → 6 страниц-data → 6 HTML:
`/ru/`, `/ru/gr/`, `/ru/gr/visa/`, `/ru/gr/visa/visa-documents/`, `/ru/gr/transport/`,
`/ru/gr/transport/airport-transfer/`.

⚠️ **Чего в новом сборщике пока НЕТ:** служебные страницы (шлюз клика, поиск, о проекте),
блок «рядом по теме» на странице, счётчик «✓ N из чатов» словами и перевод названий стран.
Всё это отдельные куски, каждый своим заходом.

### 2. Переводы остались от старой схемы

**Что не так, три места.**

1. промпт заголовков переводит **с русского** («Translate each Russian task/topic label…»),
   а имена страниц теперь английские;
2. `stamp_keys` штампует адреса: гоняет русские метки через рот RU→EN и строит хвост. Адрес
   теперь рождается в звене 4, значит и шаг, и кнопка «Адреса страниц» в пульте лишние;
3. модуль ждёт корпус старого формата (`groups`, `subshelves`) — новый корпус их не пишет,
   на нём перевод просто не соберётся.

**Как будет.** Вход — собранные страницы на английском: тексты советов (оригиналы из базы) и
имена страниц. Переводим на 14 языков, включая русский: он такой же язык перевода, как
прочие. Английская версия собирается из оригиналов бесплатно. Переводим ТОЛЬКО то, что попало
на страницы — остаток не переводим и за него не платим.

### 3. Публикации нет

**Что не так.** Кнопки в пульте нет; выкладка идёт с десктопа и последний раз доезжала в
августе. Сайт живёт снимком от 9 августа: 902 страницы против собираемых.

**Как будет.** Кнопка в пульте: рендер, карта сайта, выкладка ПОЛНЫМ снимком в каталог
раздачи. Редиректы со старых адресов не делаем — дерево меняется целиком.

### 4. Векторы берутся не у всех

**Что не так.** Выборка идёт «новее метки времени», а метка при потере файла состояния
ставится на максимум: **1 575 советов без вектора не будут взяты никогда** (замер 20.08).

**Как будет.** Выборка по признаку «у кого вектора нет». Проверка: первый прогон берёт эти
1 575, второй — ноль.

### 5. Пульт: лишний шаг и дефолты соска

**Что не так.** В меню осталась кнопка «Адреса страниц» — шаг из старой схемы. Звенья зовут
соска без модели и таймаута, берётся дефолт.

**Как будет.** Кнопку снять вместе со штамповкой. Модель и таймаут звено передаёт явно:
крупным пачкам таймаут больше, мелким меньше.

### 6. Греция размечена по-старому

**Что не так.** В `tests/tags/gr.json` лежат русские переводы и русские подтемы — разметка от
22.08, до перехода на английский.

**Как будет.** Перепрогон разметки: 751 совет, пачка 25 — **31 вызов**. Схлопывание не
трогаем, оно от языка не зависит. Дальше проход А (13 вызовов) и проход Б (9), сборка
бесплатно.

---

## Куда какая правка вмешивается

| шаг | что портит | чем чинится |
|---|---|---|
| 1 векторы | 1 575 советов без вектора навсегда | выборка по отсутствию вектора (в плане) |
| 2 схлопывание | на 0.86 за счётчиком пряталась четверть содержимого | порог 0.93 |
| 3 разметка | русский как язык структуры: лишние переводы и транслит в адресе | тема и подтема по-английски, перевода не просим |
| 4 обобщение | разнобой имён | закрытый список имён: проход А называет, проход Б только выбирает |
| 5 сборка | простыня на одном адресе | ветвление ВНУТРИ страницы по `BRANCH_MIN`, адрес один |
| 5 сборка | пыль и простыни | пороги 4 и 15 считаются после схлопывания |
| 7 публикация | сайт не равен корпусу | кнопка в пульте и полный снимок |
