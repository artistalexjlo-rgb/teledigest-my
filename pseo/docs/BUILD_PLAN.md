# Luky pSEO-билдер — план (живой, сверяется с реальностью)

> **Как жить с этим планом:** он ЖИВОЙ, не памятник. После каждого куска — сверка с
> реальностью (чекбоксы по факту, опровергнутое вычеркнуть, новую дыру вписать). Гейты —
> ИСХОДЫ (что должно стать правдой), не предписанные шаги: разведка меняет КАК, не ломая ЧТО.
> Этапы-разведки меняют то, что за ними → после них пере-планируем низ. Правила качества —
> `BUILDER_RULES.md`, тон — `MESSAGING.md`. Последняя сверка: **2026-07-17**.

Бренды: портал = **MultySpeak** (суть, домен), ассистент = **Luky** (имя на CTA).

---

## ИНВАРИАНТЫ (стабильны — меняются редко и явно)

- **Grounded-only.** Ни одного выдуманного факта; провенанс; тонкое не публикуется. → `BUILDER_RULES.md`.
- **Общий бюджет ресурсов = прод.** Gemini flash-lite и VPS-Claude делятся с продом (экстракция /
  работа юзера). Держит это **статический `RESERVE` в мозге** (cap background = RPD−RESERVE) — НЕ
  временнОе окно и НЕ чтение `gemini_quota`. → [[feedback_shared_key_budget_discipline]].
- **Мониторинг = детерминированные правила, не LLM-«Вася».** HTTP-коды/пороги/резолвится-ли —
  считаются дёшево, срабатывают сами. LLM только на реально нечёткое. → [[process_autonomous_auditor_bounds]].
- **«СКОРО» = data-backed.** Обещаем тему только если под неё есть данные ≥ порога. Состояние —
  из ДАННЫХ, не из памяти (само-сверяется). → «Инвариант навигации» ниже.
- **Обратимость / Ferrari.** Движок сам, оператор в петле, всё откатываемо (`draft`, git, STOP). → [[ferrari.md]].
- **Сам флагай дрейф.** Желаемое ≠ действительное — озвучить сразу. → [[feedback_flag_drift_proactively]].

---

## ЧТО РЕАЛЬНО ПОСТРОЕНО (сверка 2026-07-17)

- ⛔ **Билдер v2 `build.py` — МЁРТВ, файла нет** (снесён с VPS и из репо 2026-07-16 вместе с
  `batch/shelf/build2/build_brokered/facet_lang_brokered/regroup/patch_facet/facet_en`).
  Его схема (cluster → писатель → verify → topic-focus → **грунт-гейт** → **synth**) ОТВЕРГНУТА
  каноном: муха = готовый продукт, LLM только переводчик/раскладчик, без синтеза и грунт-гейта
  (`BUILDER_RULES.md` §0.1-0.3). Ниже по файлу Э3/Э5 всё ещё описаны в терминах этой мёртвой
  схемы — при следующей сверке пере-планировать.
- ✅ **Живая схема — фасет-билдер под мозгом ключей.** Карта: **`pseo/builder/ARCHITECTURE.md`**
  (сиська `keybroker.py` → сосок `call()` → 7 ртов: facet/consolidate/translate/labels/questions/
  faq/synth, у каждого дневной кап). Драйверы `runner` (facet→consolidate→questions) и
  `lang_runner` (translate).
- ⛔ **Губернатор и окно СНЕСЕНЫ (2026-07-16).** `pool_state`/`gemini_quota` как второй квота-источник
  и окно 20-23 UTC — вон. **Мозг = единственный раздатчик квоты**; коэкзистенцию с экстрактором
  держит статический `RESERVE` в мозге, не время и не чтение чужой таблицы. IPv4-форс обязателен
  в любом Gemini-HTTP ([[gotcha_ipv4_only_gemini_vps]]).
- **Навигация** `pseo/wire.py` + `pseo/demand.py`: манифест + матрица спроса (55 гео) → хабы/главная/чипы,
  soon **только data-backed** (порог 25). Само-сверяется.
- **Деплой-путь:** `pseo/deploy.sh` — «по вызову» с ДЕСКТОПА (pull собранного с VPS → demand →
  wire → render → push). **Push-кред только на десктопе, VPS безкредовый** (не раздаём допуски;
  VPS-deploy-key заблокирован как персистентность — осознанно). Билд бежит на VPS, деплой триггерю я.
  Пломбинг: FAQ JSON-LD, canonical/hreflang, per-page noindex, sitemap/robots, `/landing/` закрыт.
  ⚠️ **Правило:** ручной фикс страницы класть на VPS-источник (`out/*.json`), не только локально —
  иначе `deploy.sh` затрёт его при pull (дрейф источника).
- **Наблюдаемость:** маячок билдера (`status.json`/`audit.log`/`STOP`/чирик-при-сбое) +
  **детерм. sentinel** (`sentinel.py`, cron 6ч: битые ссылки/health/builder/quota).
- **В сети (noindex):** 8 авто-страниц + br/finance, 6 гео-хабов + главная. Adversarial-ревью
  8 страниц: 6/8 чисто, 2 смысловых дефекта пойманы+починены.

---

## ЭТАПЫ (гейт = исход; статус по факту)

**Э0-1. База + первая страница** — ✅. Портал, шаблоны, 2-й контур bge-m3, деплой-смоук.

**Э2. Карта спроса** — ✅ (в объёме исхода). *Исход:* видно, какие geo×topic page-ready и где дыры.
`demand.py` даёт матрицу (55 гео × темы), `TAG2TOPIC` чистит грязный тег в ~7 канонов, порог 25.
*Долг:* уникальные-Q/median-source не считаю (взял counts) — если понадобится тоньше, добрать.

**Э3. Узел-экстрактор** — ✅ (главный гейт качества пройден). *Исход:* авто-страница не хуже
ручного эталона, 0 выдуманного. Механизм: писатель(flash-lite) → детерм-verify → topic-focus →
грунт-гейт(claude). Ревью 8 страниц подтвердил grounded (2 дефекта пойманы). Провенанс: `n`=число
цитат на FAQ (не строго «≥2 на факт» — аспирация, не механизм; честно).

**Э4. Лонг-тейл (Travel-помойки)** — 🟡 разведан, не в продакшне. Проба `br/Travel` → 12 чистых
под-тем (жёлтая лихорадка, Игуасу, GRU…). *Исход-гейт:* отобранные пары «узкий интент × grounded ×
низкая конкуренция». *Осталось:* прогнать под-кластеры через билдер как страницы-единицы.

**Э4.5 (НОВЫЙ, был пропущен). Сторож живого сайта — ПЕРЕД масштабом** — ✅ база.
*Почему тут:* нельзя лить объём без контроля опубликованного (иначе копим слепую гниль — так и
всплыло). *Исход:* битьё/тухлые ссылки ловятся автоматом. `sentinel.py` (детерм., cron 6ч):
обход ссылок + health + builder + quota. *Осталось:* **свежесть** (page.updated vs рост данных
ячейки → пересобрать) + Search-Console-сигналы (нужен аккаунт юзера).

**Э5. Пакетная генерация (масштаб)** — 🟡 начат (8 страниц). *Гейт на КАЖДУЮ страницу:* провенанс,
0 выдуманного, связный текст, ≥5-6 FAQ (широкая) / ≥1 тугой (лонг-тейл), 0 битых ссылок. Тонкое-без-
данных — парк, и **логировать пропуски** (иначе «200 страниц» = мнимое покрытие). *Осталось:* расширить
`cells.json` под все data-backed темы + Э4-лонг-тейл, гонять под присмотром sentinel, вне вечернего окна.

**Э6. Свежесть-конвейер** — 🟡 частично. Деплой-путь и наблюдаемость ✅. *Осталось:* дельта-пересборка
(только изменившиеся кластеры), diff-видимость. *Исход:* новое в grab за неделю доезжает до страниц
(иначе фантом).

**Э7. Замер и воронка** — ⬜ (нужен юзер). Search Console (индексация/показы/лонг-тейл) + счётчик
CTA→Luky. Предусловие: **флип `draft=False`** после ревью + разбор CF-managed-robots (чтоб sitemap
дошёл до Google).

---

## Инвариант навигации — «СКОРО» не протухает — ✅ выполнен

1. **«СКОРО» = data-backed.** `wire.py` читает `demand.json` (счётчики VPS), soon-плитка только при
   данных ≥25; ниже — плитки нет (нет ложных обещаний). gr-хаб: только bureaucracy (fin/safety тонки).
2. **Состояние — из данных**, само-сверяется на каждом `wire.py`. Не в памяти Claude.
3. **Сторож сверяет протухание** — ⬜ TODO: «СКОРО» с данными но без страницы N дней → «собери» в канал;
   без данных → демоут. (Ждёт добавления в `sentinel.py`.)

---

## Светофор (быстрый взгляд)

| Этап | Статус | Готово, если… |
|------|--------|----------------|
| 2 Карта | ✅ | видно page-ready и дыры |
| 3 Узел | ✅ | авто ≥ эталон, 0 вранья (ревью подтвердил) |
| 4 Лонг-тейл | 🟡 разведан | узкие пары с данными отобраны |
| 4.5 Сторож сайта | ✅ база | битьё ловится (свежесть — долг) |
| 5 Масштаб | 🟡 8 стр | высокая доля прошла гейт, пропуски логируются |
| 6 Свежесть | 🟡 | новое доезжает до страниц |
| 7 Воронка | ⬜ | показы+клики по лонг-тейл (нужен GSC+флип) |

**Ближайший осмысленный шаг:** масштаб (Э5) — сторож (Э4.5) уже прикрывает битьё; «СКОРО»→живые
зажгутся сами. Долги-хвосты (свежесть, протухшие-СКОРО в sentinel, лонг-тейл в прод) — параллельно.
