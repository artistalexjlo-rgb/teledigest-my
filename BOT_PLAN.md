# План починки бота-сборщика (временный, идём по нему)

Заведён 2026-08-24 после разбора логов экстрактора и дайджеста. Файл рабочий: пункт сделан —
вычёркиваем; всё сделано — файл удаляем. Тракт страниц сюда НЕ входит, он в `pseo/docs/PLAN.md`.

⭐ **Порядок неслучаен.** Сперва снимаем холостую нагрузку (этап 1), потом переезжаем на мозг
(этап 2). Переезд сам лечит 429, ротацию ключей и таймаут — поэтому чинить их в экстракторе
ОТДЕЛЬНО не надо: этот код уезжает целиком.

---

## Этап 1. Холостая нагрузка — снять до переезда

### 1.1. Пустой файл не создаётся, а накопленное удаляется ⛔ главное

Дамп пишет файл на каждый источник за каждый день, даже когда сообщений ноль: получается
74 байта одного заголовка `# country=... messages=0`. Экстрактор шлёт его в Gemini.

Маркер `.processed` ставится ТОЛЬКО при сохранённых паттернах (`if attempted > 0`), поэтому
пустышка не помечается никогда и возвращается в очередь на КАЖДОМ проходе.

**Замер 23.08:** необработанных файлов 166, из них **120 пустых** (72%); всего проб 4 038,
пустых 194 (74 из них с маркером). Копятся помесячно: май 13 → июнь 29 → июль 66 → август 85.
Отсюда «раньше не троило»: код прежний, холостых вызовов в девять раз больше.

**Делаем три вещи, в этом порядке:**

1. **`dump_country_samples` не создаёт файл, когда сообщений ноль** — это корень: без него
   пустышки родятся снова, что бы мы ни чистили;
2. **удаляем накопленные 194 пустых файла** вместе с их сайдкарами `.processed`. Не помечаем,
   а удаляем: помеченная пустышка — это два бесполезных файла вместо нуля. Читает их только
   экстрактор (выгрузка в Drive в конфиге не включена), терять нечего: содержимое файла —
   одна строка заголовка, сами сообщения лежат в базе и файл пересоздаётся дампом;
3. **забор в экстракторе:** файл без тела в модель не отправляем. Одна проверка перед
   вызовом — страховка на случай, если пустышка появится другим путём.

Тихие каналы — норма, а не поломка: у `ve` за всё время 186 сообщений, у `co` 685, `mg` 813,
`cl` 1044, `hr` 1245; новый `brazil_lifehacks` пишет редко. Пустой день у них обычное дело.

### 1.2. Дайджест: повтор при сетевом сбое, и не публиковать ошибку в канал

23.08 в 20:31 UTC провайдерский DNS `199.195.255.68` перестал отвечать: в журнале хоста
**119 ошибок резолвера, все в одном часе**. Дайджест попал в окно, повтора у него нет —
и в канал уехал текст `Failed to generate AI summary … Connection error`.

Всплеск не одиночный: 19.08 — 199 ошибок, 20–22.08 — ноль, 23.08 — 119.

**Делаем:** повтор с паузой (сеть — не вина материала), а при неудаче молчать: канал не место
для трассы ошибки.

---

## Этап 2. Экстрактор — на мозг (`keybroker`)

Смысл переезда: **учёт ключей ОДИН на всех едоков**. Сейчас у экстрактора своя ротация, своя
таблица `gemini_quota` и своя политика 429 — это второй учёт на тех же ключах, тот самый
«осьминог», из-за которого ключи горели.

### 2.1. Маунт `/root/pseo_builder` в контейнер `bots-grab` ⛔ блокирующий, инфра

Мозг держит состояние в `/root/pseo_builder/keybroker.db` (путь переопределяется `KB_DB`).
Контейнер бота монтирует только `/config`, `/data`, `/home/teledigest/data`. У пульта такой
маунт уже есть — делаем так же. Без него счёт останется раздельным и переезд бессмысленен.

### 2.2. Вызов через сосок

Вместо `_gemini_generate_json(content, model, api_key)` — `keybroker.call(user, sysprompt,
consumer="extract", model=…, timeout=…, salvage=…)`. Сосок сам: выдаёт ключ, держит такт,
отрабатывает 429 по лестнице, повторяет, спасает битый JSON.

### 2.3. Снести свою машинерию экстрактора

`iter_model_key_pairs`, таблицу `gemini_quota`, `quota_ban_today`, собственные ретраи.
Оставлять параллельно мозгу нельзя — это и есть второй учёт.

⛔ Отдельно НЕ чиним (уезжает вместе с кодом):
- мгновенный дневной бан пары на первом 429 (сегодня так умерли **12 пар из 48** при шести
  запросах на ключ — это не исчерпание RPD 500, а rate-стена). У мозга правило другое:
  метка → лестница 60/300/1800/6000 → бан только после лестницы;
- ротация «12 ключей подряд по одной модели» (замер: 4 ключа на том же темпе — ноль отказов,
  12 — стена).

### 2.4. Завести потребителя `extract` в мозге и дать ему кап

У остальных ртов кап есть: facet 1500 · carve 300 · assign 300 · kratko 600 · translate 400.
**Число выбирает юзер.**

### 2.5. Модели — из вызова, свои капы не тащить

Сейчас у экстрактора свои три с капами: `gemini-3.1-flash-lite` 500, `gemini-2.5-flash` 20,
`gemini-2.5-flash-lite` 20. У мозга лимиты по моделям уже записаны в `LIMITS`.

### 2.6. Таймаут — параметром вызова

60 секунд мало крупным файлам: в очереди есть 145–178 КБ, и `2026-08-19_kr_1001776072744.txt`
потерян после четырёх попыток по минуте. У соска `timeout` есть, задаём по размеру файла.

---

## Не входит в этот план

- **Эмбеддинг** (`gemini-embedding-2`): 192 попытки за 23.08, банов ноль — работает, переезд
  отдельным заходом.
- **Мёртвый контейнер `o14dukiwemhrq7vs5twm1jj2`** (TaskBot, aiogram): 142 248 перезапусков с
  мая, падает на `api.telegram.org` — сеть `coolify`, которой в докере больше нет. Юзер
  разбирается отдельной сессией.
- **Запасной резолвер на хосте** (`/etc/resolv.conf.tail`): сейчас в `resolv.conf` два адреса
  одного провайдера, и просадка кладёт всё сразу. Операция на сервере, по слову юзера.
